# Django Settings
SECRET_KEY=your_django_secret_key_here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# AI/ML API Keys
HUGGINGFACE_API_KEY=your_huggingface_api_key_here_optional
OPENAI_API_KEY=your_openai_api_key_for_whisper_optional

# Email Configuration (for magic links)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
DEFAULT_FROM_EMAIL=Saathi <your_email@gmail.com>

# Firebase (optional for server-side verification)
FIREBASE_SERVICE_ACCOUNT=path/to/serviceAccountKey.json

# Database (default SQLite, can switch to PostgreSQL for production)
DATABASE_URL=sqlite:///db.sqlite3

# Storage paths
MEDIA_ROOT=media/
STATIC_ROOT=static/
FAISS_INDEX_PATH=data/faiss_index/
CHROMA_PERSIST_DIR=data/chroma_db/

# CORS settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000